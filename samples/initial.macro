["RunMode": "Worker"] 
# Debug = Run as debug with diagnostics window
# Worker = Run as a worker service
# App = Run as an application
# TrayApp = Run as an application in the system tray (Windows only)

move_mouse to ScreenPosition(50, 50);
click MouseButton.Left for Seconds(1);
# is the same as...
click MouseButton.Left at ScreenPosition(50, 50) for Seconds(1);